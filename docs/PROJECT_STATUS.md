# Collab Hub — Статус проекта

> Последнее обновление: 2026-02-27

---

## 🌐 Деплой

- **Prod URL:** https://collab-mvp-app.onrender.com
- **Git репо:** https://github.com/Havanide/collab_hub
- **Платформа:** Render.com (бесплатный тариф — сервис засыпает после простоя)

---

## 🧱 Технический стек

| Слой | Технология |
|------|------------|
| Frontend | React 18 + TypeScript + Vite |
| Роутинг | React Router DOM v6 |
| Backend | Express.js (ESM) |
| База данных | JSON-файл (`server/data/db.json`) |
| Аутентификация | JWT (httpOnly cookie, 7 дней) |
| Хеширование паролей | bcryptjs |
| Загрузка файлов | multer |
| ID генерация | nanoid |

---

## 📁 Структура проекта

```
collab-mvp-app-render/
├── client/                    # React-фронтенд (Vite)
│   ├── public/
│   │   └── logo.png           # Логотип (бесконечность, прозрачный фон)
│   └── src/
│       ├── components/
│       │   ├── TopNav.tsx         # Хедер с навигацией, лого, бургер-меню
│       │   ├── Footer.tsx         # Футер со ссылкой Privacy
│       │   ├── ListingCard.tsx    # Карточка объявления
│       │   ├── ProposeProjectForm.tsx  # Форма отправки заявки (реюз)
│       │   ├── Modal.tsx
│       │   ├── Toast.tsx
│       │   └── CookieBanner.tsx
│       ├── pages/
│       │   ├── LoginPage.tsx
│       │   ├── RegisterPage.tsx
│       │   ├── HomePage.tsx       # Главная: поиск + пагинация + кнопка матча
│       │   ├── FiltersPage.tsx    # Расширенный поиск + кнопка матча
│       │   ├── ProjectsPage.tsx   # Мои проекты + кнопка матча
│       │   ├── ListingPage.tsx    # Карточка объявления
│       │   ├── MyProfilePage.tsx  # Личный кабинет (редактирование)
│       │   ├── PublicProfilePage.tsx
│       │   ├── RequestsPage.tsx   # Входящие/исходящие заявки
│       │   ├── MatchesPage.tsx    # Активные матчи
│       │   ├── MatchDetailsPage.tsx
│       │   ├── MatchSwipePage.tsx # 🆕 Тиндер-режим матчинга
│       │   └── PrivacyPage.tsx
│       ├── styles/
│       │   ├── global.css         # Основные стили
│       │   └── theme.css          # Цветовая палитра CSS-переменные
│       ├── api.ts                 # API-клиент + типы данных
│       ├── auth.tsx               # AuthContext (me, login, logout, refresh)
│       └── App.tsx                # Роутинг
│
├── server/
│   ├── data/
│   │   └── db.json               # JSON БД (сбрасывается при перезапуске Render)
│   └── src/
│       ├── index.js              # Express-сервер, все эндпоинты
│       ├── auth.js               # JWT helpers, cookie helpers
│       ├── store.js              # loadDb / saveDb / resetDb / normalizeDb
│       └── seed.js               # seedIfEmpty — 15 тестовых пользователей
│
├── docs/
│   └── PROJECT_STATUS.md         # Этот файл
│
└── .claude/
    └── launch.json               # Dev-серверы для Claude Preview
```

---

## 🗺️ Роутинг (клиент)

| Путь | Компонент | Доступ |
|------|-----------|--------|
| `/` | → редирект `/app/home` | — |
| `/login` | LoginPage | Публичный |
| `/register` | RegisterPage | Публичный |
| `/privacy` | PrivacyPage | Публичный |
| `/app/home` | HomePage | 🔒 Авторизован |
| `/app/filters` | FiltersPage | 🔒 |
| `/app/projects` | ProjectsPage | 🔒 |
| `/app/requests` | RequestsPage | 🔒 |
| `/app/matches` | MatchesPage | 🔒 |
| `/app/match/:id` | MatchDetailsPage | 🔒 |
| `/app/match-swipe` | MatchSwipePage | 🔒 |
| `/app/listing/:id` | ListingPage | 🔒 |
| `/app/profile` | MyProfilePage | 🔒 |
| `/app/seller/:userId` | PublicProfilePage | 🔒 |

---

## 🔌 API эндпоинты (сервер)

### Auth
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/auth/register` | Регистрация |
| POST | `/api/auth/login` | Вход |
| POST | `/api/auth/logout` | Выход |
| GET | `/api/me` | Текущий пользователь |

### Профиль
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/profile/me` | Полный профиль (с приватными данными) |
| PUT | `/api/profile/me` | Обновить профиль |
| POST | `/api/profile/me/logo` | Загрузить логотип |
| GET | `/api/profile/:userId` | Публичный профиль + объявления |

### Объявления (listings)
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/listings` | Поиск (q, kind, region, marketplace...) |
| GET | `/api/listings/mine` | Мои объявления |
| GET | `/api/listings/:id` | Одно объявление |
| POST | `/api/listings` | Создать |
| PUT | `/api/listings/:id` | Обновить |
| POST | `/api/listings/:id/publish` | Опубликовать |
| POST | `/api/listings/:id/unpublish` | Снять |
| DELETE | `/api/listings/:id` | Удалить |

### Заявки (match_requests)
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/match-requests/incoming` | Входящие |
| GET | `/api/match-requests/outgoing` | Исходящие |
| POST | `/api/match-requests` | Создать заявку |
| POST | `/api/match-requests/:id/accept` | Принять (to_user) |
| POST | `/api/match-requests/:id/confirm` | Подтвердить → Match (from_user) |
| POST | `/api/match-requests/:id/reject` | Отклонить |
| POST | `/api/match-requests/:id/cancel` | Отменить |

### Матчи
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/matches` | Список матчей |
| GET | `/api/matches/:id` | Детали (с контактами если active) |
| POST | `/api/matches/:id/terminate` | Завершить |

### Служебные
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/admin/reset` | Сброс БД + пересев (x-reset-secret header) |

---

## 🎨 Цветовая палитра

```css
--wb-midnight: #0F0A1E   /* Тёмно-фиолетовый (фон хедера/свайпа) */
--wb-accent:   #6E1FF7   /* Основной фиолетовый (кнопки, акценты) */
--wb-pink:     #C91472   /* Розово-фиолетовый (градиенты) */
--bg:          #f6f2f7   /* Светло-серый (фон страниц) */
--card:        #ffffff   /* Белый (карточки) */
--text:        #14121a   /* Почти чёрный (основной текст) */
--muted:       #6b7280   /* Серый (вспомогательный текст) */
```

---

## 👥 Тестовые аккаунты

> **Пароль для всех:** `demo1234`

| Роль | Email |
|------|-------|
| Партнёрства | `collab1@collab.local` … `collab5@collab.local` |
| Услуги | `service1@collab.local` … `service5@collab.local` |
| Поставщики | `supplier1@collab.local` … `supplier5@collab.local` |

Форма логина предзаполнена `collab1@collab.local`.

### ⚠️ Если аккаунты не работают (сброс БД):
```bash
curl -X POST https://collab-mvp-app.onrender.com/api/admin/reset \
  -H "x-reset-secret: my-secret-123"
# Ответ: {"ok":true,"users":15,"listings":15}
```

---

## ✅ Реализованный функционал (хронология)

| Дата | Фича |
|------|------|
| — | Базовая авторизация (register / login / logout) |
| — | Создание и поиск объявлений (listings) |
| — | 2-шаговый матчинг: заявка → принять → подтвердить → контакты открыты |
| — | Trust Score (автоматический расчёт по заполненности профиля) |
| — | Privacy Page перенесена в Footer |
| — | Цветовая палитра обновлена (#0F0A1E, #6E1FF7, #C91472) |
| — | 15 тестовых аккаунтов в seed.js |
| — | Сброс БД через `/api/admin/reset` |
| — | Пагинация на главной (10 карточек/страница) |
| — | Sticky footer (flex layout) |
| — | Бургер-меню в TopNav (мобильная версия) |
| — | Логотип (бесконечность) в хедере с прозрачным фоном |
| — | Имя профиля в хедере — кликабельно, жирное, ведёт на /app/profile |
| 2026-02-27 | 🆕 MatchSwipePage — Тиндер-режим матчинга |
| 2026-02-27 | 🆕 Кнопка «🎯 Найти матч» в Home / Filters / Projects |
| 2026-02-27 | 🆕 ProposeProjectForm вынесен в отдельный компонент |

---

## 🚧 Известные ограничения (MVP)

- **JSON БД**: Render сбрасывает файлы при каждом деплое → нужен `reset` после перезапуска
- **Нет real-time**: Уведомления о заявках/матчах не приходят — нужно обновлять страницу
- **Нет e-mail**: Регистрация без подтверждения почты
- **Лимиты**: Максимум 3 проекта на пользователя, 2MB на логотип
- **Свайп**: Пропущенные карточки не запоминаются (каждая сессия с начала)

---

## 🔧 Локальная разработка

```bash
# Установить зависимости
npm run install:all

# Запустить оба сервера
npm run dev         # или отдельно:
npm run dev:client  # Vite → http://localhost:5173
npm run dev:server  # Express → http://localhost:5174
```

Dev-серверы настроены в `.claude/launch.json`.
